# Option 1
# Redirecting to the host
$ ffuf -u https://<ip> -w my_wordlist -H "Host: FUZZ.mydomain.com" -f <in filter use the first word of the event you want to filter and space and value of that event) to make it quick and identical>

